// doesn't work at all
#include <bits/stdc++.h>
using namespace std;

typedef long long ll;
int n, m;


int main()
{
    scanf("%d %d", &n, &m);
    gen(1, vector<int>());
    printf("%lld\n", result);
}
